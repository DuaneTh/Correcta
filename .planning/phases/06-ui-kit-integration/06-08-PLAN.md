---
phase: 06-ui-kit-integration
plan: 08
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - components/admin/school/CourseFormModal.tsx
autonomous: true

must_haves:
  truths:
    - "CourseFormModal form fields use Input/Select/Textarea from UI Kit Form"
    - "CourseFormModal buttons use Button component instead of raw HTML"
    - "CourseFormModal layout uses Stack and Inline for consistent spacing"
  artifacts:
    - path: "components/admin/school/CourseFormModal.tsx"
      provides: "Migrated course form modal (1840 lines)"
      contains: "import.*Input.*from.*@/components/ui/Form"
  key_links:
    - from: "components/admin/school/CourseFormModal.tsx"
      to: "components/ui/Form.tsx"
      via: "form component imports"
      pattern: "import.*from.*@/components/ui/Form"
    - from: "components/admin/school/CourseFormModal.tsx"
      to: "components/ui/Button.tsx"
      via: "button imports"
      pattern: "import.*Button.*from.*@/components/ui/Button"
---

<objective>
Migrate CourseFormModal.tsx (1840 lines) — the largest single component in the codebase — to UI Kit.

Purpose: This massive form modal handles course creation/editing with many form fields. It deserves its own plan due to size and complexity. Completing this finishes all shared component migrations.

Output: CourseFormModal fully migrated to UI Kit components with all form functionality preserved.
</objective>

<execution_context>
@C:\Users\hugol\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hugol\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@components/ui/Button.tsx
@components/ui/Card.tsx
@components/ui/Text.tsx
@components/ui/Layout.tsx
@components/ui/Form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate CourseFormModal.tsx structural elements (top half)</name>
  <files>components/admin/school/CourseFormModal.tsx</files>
  <action>
    Read the full file first. This is a ~1840-line form modal. Plan a systematic migration:

    1. **Add imports** at the top:
       ```tsx
       import { Button } from '@/components/ui/Button'
       import { Card, CardBody } from '@/components/ui/Card'
       import { Text } from '@/components/ui/Text'
       import { Stack, Inline } from '@/components/ui/Layout'
       import { Input, Select, Textarea } from '@/components/ui/Form'
       import { Badge } from '@/components/ui/Badge'
       ```

    2. **Replace form elements** throughout:
       - All `<input className="rounded-md border...">` → `<Input>` (preserve all value/onChange/name/type/placeholder/required props)
       - All `<select className="...">` → `<Select>` (preserve options, value, onChange)
       - All `<textarea className="...">` → `<Textarea>` (preserve rows, value, onChange)
       - All `<label className="...text-sm font-medium...">` → `<Text as="label" variant="label">`
       - All `<button className="...bg-brand-900...">` → `<Button variant="primary">`
       - All `<button className="...border border-gray...">` → `<Button variant="secondary">`
       - All `<button>` with trash/delete → `<Button variant="destructive">` or `<Button variant="ghost">`

    3. **Replace layout elements**:
       - `<div className="flex flex-col gap-N">` → `<Stack gap="...">`
       - `<div className="flex items-center justify-between">` → `<Inline align="between">`
       - `<div className="grid grid-cols-2 gap-N">` → `<Grid cols="2" gap="...">`
       - Section headers → `<Text variant="sectionTitle">`
       - Helper text → `<Text variant="caption">`

    4. **Replace container elements**:
       - Card-like sections within the form → `<Card><CardBody>` or `<Surface>`
       - Divider sections → `<Spacer>` or keep `<hr>` if used

    IMPORTANT RULES:
    - Do NOT change any form logic (validation, submission, state management)
    - Do NOT change the Drawer/modal wrapper if it uses existing Drawer component
    - Do NOT modify complex interactive widgets (date pickers, rich text editors, image upload) — only migrate their surrounding containers
    - Preserve all error message displays, just wrap in Text variant="caption" with className for red
    - If the modal uses tabs or multi-step form, keep the tab logic, just replace tab buttons with Button or SegmentedControl
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. File imports from @/components/ui/. No raw `<input className=` patterns remain (except within complex widgets like DateInput/ImageUpload).</verify>
  <done>CourseFormModal.tsx fully migrated to UI Kit. All form fields use Input/Select/Textarea. All buttons use Button component. Layout uses Stack/Inline/Grid. Form logic and Drawer wrapper preserved.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. CourseFormModal imports from @/components/ui/*
3. All form inputs use Input/Select/Textarea from Form
4. All buttons use Button component
5. Form submission, validation, and state management unchanged
6. Drawer wrapper behavior preserved
</verification>

<success_criteria>
- CourseFormModal (1840 lines) fully migrated to UI Kit
- All form fields consistent with UI Kit Form components
- No raw HTML form/button patterns remaining
- Form functionality completely preserved
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-kit-integration/06-08-SUMMARY.md`
</output>
