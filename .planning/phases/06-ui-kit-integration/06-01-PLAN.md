---
phase: 06-ui-kit-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/Button.tsx
  - components/ui/Card.tsx
  - components/ui/Text.tsx
  - components/ui/Layout.tsx
  - components/ui/Badge.tsx
  - components/ui/StatusBadge.tsx
  - components/ui/StatPill.tsx
  - components/ui/EmptyState.tsx
  - components/ui/Form.tsx
  - components/ui/SearchField.tsx
  - components/ui/SegmentedControl.tsx
  - components/ui/TextLink.tsx
  - components/ui/UiKitReference.tsx
  - components/ui/cn.ts
autonomous: true

must_haves:
  truths:
    - "All UI Kit components from kourpat1 are importable on current branch"
    - "Existing components (ConfirmModal, Drawer, CsvUploader, ImageUpload, DateInput, DateTimePicker) still work"
    - "TypeScript compilation passes with new components present"
  artifacts:
    - path: "components/ui/Button.tsx"
      provides: "Button component with primary|secondary|ghost|destructive variants"
    - path: "components/ui/Card.tsx"
      provides: "Card, CardHeader, CardBody components"
    - path: "components/ui/Text.tsx"
      provides: "Text component with pageTitle|sectionTitle|body|muted|caption|overline|xsMuted|label variants"
    - path: "components/ui/Layout.tsx"
      provides: "Stack, Inline, Grid, Surface, Container, Spacer, Section components"
    - path: "components/ui/Badge.tsx"
      provides: "Badge component with neutral|info|success|warning variants"
    - path: "components/ui/Form.tsx"
      provides: "Input, Textarea, Select components"
    - path: "components/ui/cn.ts"
      provides: "cn() utility for class name merging"
  key_links:
    - from: "components/ui/SearchField.tsx"
      to: "components/ui/Form.tsx"
      via: "imports Input"
      pattern: "import.*Input.*from.*Form"
    - from: "components/ui/StatusBadge.tsx"
      to: "components/ui/Badge.tsx"
      via: "imports Badge"
      pattern: "import.*Badge.*from.*Badge"
---

<objective>
Bring all UI Kit components from the feat/kourpat1 branch into the current working branch.

Purpose: The UI Kit components (Button, Card, Text, Layout, Badge, Form, etc.) exist on remotes/origin/feat/kourpat1 but NOT on the current dx/frontend-csrf-helper branch. All subsequent migration plans depend on these components being available for import.

Output: All 14 UI Kit files from kourpat1 added to components/ui/ alongside existing components (ConfirmModal, Drawer, etc.), with the build passing.
</objective>

<execution_context>
@C:\Users\hugol\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hugol\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick UI Kit component files from kourpat1</name>
  <files>
    components/ui/Button.tsx
    components/ui/Card.tsx
    components/ui/Text.tsx
    components/ui/Layout.tsx
    components/ui/Badge.tsx
    components/ui/StatusBadge.tsx
    components/ui/StatPill.tsx
    components/ui/EmptyState.tsx
    components/ui/Form.tsx
    components/ui/SearchField.tsx
    components/ui/SegmentedControl.tsx
    components/ui/TextLink.tsx
    components/ui/UiKitReference.tsx
    components/ui/cn.ts
  </files>
  <action>
    Extract the UI Kit files from the kourpat1 branch using git show and write them to the working tree. Do NOT use git cherry-pick (the kourpat1 commit includes many unrelated changes). Instead, for each file:

    ```bash
    git show remotes/origin/feat/kourpat1:components/ui/FILENAME > components/ui/FILENAME
    ```

    Files to extract (14 total):
    - cn.ts (utility - no dependencies)
    - Button.tsx (depends on cn)
    - Card.tsx (depends on cn)
    - Text.tsx (depends on cn)
    - Layout.tsx (depends on cn)
    - Badge.tsx (depends on cn)
    - StatusBadge.tsx (depends on Badge)
    - StatPill.tsx (standalone)
    - EmptyState.tsx (depends on cn)
    - Form.tsx (depends on cn) — Input, Textarea, Select
    - SearchField.tsx (depends on Form/Input)
    - SegmentedControl.tsx (depends on cn)
    - TextLink.tsx (depends on cn)
    - UiKitReference.tsx (showcase component)

    IMPORTANT: Do NOT overwrite existing files that are NOT on kourpat1:
    - ConfirmModal.tsx (exists on current branch only)
    - Drawer.tsx (exists on current branch only)
    - CsvUploader.tsx (exists on current branch only)
    - ImageUpload.tsx (exists on current branch only)
    - DateInput.tsx (exists on current branch, different version on kourpat1)
    - DateTimePicker.tsx (exists on current branch, NOT on kourpat1)

    After extracting, verify no BOM characters (kourpat1 files have UTF-8 BOM). Strip BOM if present using sed or similar.
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify TypeScript compilation passes.
    Run `ls components/ui/` to confirm all 20 files exist (14 new + 6 existing).
    Verify imports work: check that SearchField.tsx can resolve Form.tsx import, StatusBadge.tsx can resolve Badge.tsx import.
  </verify>
  <done>
    All 14 UI Kit component files from kourpat1 exist in components/ui/ alongside the 6 existing files. TypeScript compilation passes. No import resolution errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add UI Kit showcase route</name>
  <files>
    app/internal/ui-kit/page.tsx
  </files>
  <action>
    Check if kourpat1 has an /internal/ui-kit page route:
    ```bash
    git show remotes/origin/feat/kourpat1:app/internal/ui-kit/page.tsx
    ```

    If it exists, extract it the same way. If not, create a simple page that imports and renders UiKitReference component:

    ```tsx
    import { UiKitReference } from '@/components/ui/UiKitReference'

    export default function UiKitPage() {
      return <UiKitReference />
    }
    ```

    Also check if kourpat1 has the TeacherCoursesClient.tsx already migrated. If so, extract it as well — it serves as the reference implementation pattern for all other migrations.

    ```bash
    git show remotes/origin/feat/kourpat1:app/teacher/courses/TeacherCoursesClient.tsx > app/teacher/courses/TeacherCoursesClient.tsx
    ```

    Verify the page renders by checking the build completes.
  </action>
  <verify>
    Run `npx next build` (or `npx tsc --noEmit` if build is slow) to confirm no compilation errors.
    Confirm app/internal/ui-kit/page.tsx exists.
    Confirm TeacherCoursesClient.tsx is updated with UI Kit imports if kourpat1 version differs.
  </verify>
  <done>
    /internal/ui-kit route exists and renders the UI Kit showcase. TeacherCoursesClient.tsx is the reference implementation using all UI Kit components. Build passes clean.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. All 14 UI Kit files exist in components/ui/
3. Existing components (ConfirmModal, Drawer, etc.) unchanged
4. /internal/ui-kit page route exists
</verification>

<success_criteria>
- All UI Kit components importable from @/components/ui/*
- Build passes with no TypeScript errors
- Existing functionality unaffected
- TeacherCoursesClient serves as reference migration pattern
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-kit-integration/06-01-SUMMARY.md`
</output>
