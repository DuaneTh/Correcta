---
phase: 06-ui-kit-integration
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/admin/school/enrollments/SchoolEnrollmentsClient.tsx
  - app/admin/school/dashboard/SchoolDashboardClient.tsx
  - app/admin/school/settings/SchoolSettingsClient.tsx
  - app/admin/platform/dashboard/DashboardClient.tsx
  - app/admin/platform/ai/AIConfigClient.tsx
  - app/admin/platform/system/SystemSettingsClient.tsx
  - components/admin/platform/PlatformAdminLayout.tsx
  - components/admin/school/SchoolAdminLayout.tsx
autonomous: true

must_haves:
  truths:
    - "All small admin pages use UI Kit components instead of raw HTML"
    - "Platform admin dashboard uses Card+StatPill for metrics display"
    - "Admin layout components use UI Kit primitives for navigation"
  artifacts:
    - path: "app/admin/school/dashboard/SchoolDashboardClient.tsx"
      provides: "Migrated school dashboard with StatPill metrics"
      contains: "import.*StatPill.*from.*components/ui/StatPill"
    - path: "app/admin/platform/dashboard/DashboardClient.tsx"
      provides: "Migrated platform dashboard"
      contains: "import.*Card.*from.*components/ui/Card"
    - path: "components/admin/platform/PlatformAdminLayout.tsx"
      provides: "Migrated platform admin layout"
      contains: "import.*from.*@/components/ui"
  key_links:
    - from: "app/admin/school/dashboard/SchoolDashboardClient.tsx"
      to: "components/ui/StatPill.tsx"
      via: "import"
      pattern: "import.*StatPill"
---

<objective>
Migrate the remaining smaller admin pages (6 pages) and the 2 admin layout components to UI Kit.

Purpose: Completes the admin area migration. These pages are smaller (200-430 lines each) so they can be batched efficiently. The layout components establish consistent admin navigation.

Output: All 8 files migrated to UI Kit components, completing UIKIT-02 requirement.
</objective>

<execution_context>
@C:\Users\hugol\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hugol\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/teacher/courses/TeacherCoursesClient.tsx (reference implementation)
@components/ui/Button.tsx
@components/ui/Card.tsx
@components/ui/Text.tsx
@components/ui/Layout.tsx
@components/ui/StatPill.tsx
@components/ui/Form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate school admin small pages (4 files)</name>
  <files>
    app/admin/school/enrollments/SchoolEnrollmentsClient.tsx
    app/admin/school/dashboard/SchoolDashboardClient.tsx
    app/admin/school/settings/SchoolSettingsClient.tsx
    components/admin/school/SchoolAdminLayout.tsx
  </files>
  <action>
    Apply UI Kit migration to each file following TeacherCoursesClient.tsx patterns:

    **SchoolEnrollmentsClient.tsx (431 lines):**
    - Enrollment list/table → Card + CardBody for each row or Surface for table container
    - Action buttons → Button variants
    - Search/filter → SearchField + Select
    - Status indicators → Badge variants
    - Page header → Text variant="pageTitle" + Inline

    **SchoolDashboardClient.tsx (272 lines):**
    - Dashboard metrics cards → Card + CardBody wrapping StatPill, or Grid of Card components
    - Section titles → Text variant="sectionTitle"
    - Stats numbers → Text with appropriate variant
    - Quick action buttons → Button
    - Layout → Stack for vertical sections, Grid for metric cards

    **SchoolSettingsClient.tsx (248 lines):**
    - Settings form → Stack of Input/Select/Textarea from Form
    - Section dividers → Text variant="sectionTitle" + Surface
    - Save/cancel buttons → Button variant="primary" and Button variant="secondary"
    - Form labels → Text variant="label"

    **SchoolAdminLayout.tsx (114 lines):**
    - Navigation links → TextLink or Button variant="ghost" for nav items
    - Layout wrapper → Stack or Container
    - Active nav indicator → cn() for conditional styling
    - Keep the layout structure but replace raw HTML elements
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. All 4 files import from @/components/ui/.</verify>
  <done>All 4 school admin files fully migrated to UI Kit. No raw HTML button/card patterns remain.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate platform admin pages (4 files)</name>
  <files>
    app/admin/platform/dashboard/DashboardClient.tsx
    app/admin/platform/ai/AIConfigClient.tsx
    app/admin/platform/system/SystemSettingsClient.tsx
    components/admin/platform/PlatformAdminLayout.tsx
  </files>
  <action>
    Apply UI Kit migration to each file:

    **DashboardClient.tsx (207 lines):**
    - Platform stats → Grid of Card + CardBody with StatPill or Text for numbers
    - Quick links/actions → Button or TextLink
    - Page layout → Container + Stack

    **AIConfigClient.tsx (408 lines):**
    - AI configuration form → Stack of Input/Select/Textarea from Form
    - Model selection → Select from Form
    - Temperature/parameter inputs → Input from Form
    - Section grouping → Card + CardBody for each config section
    - Save button → Button variant="primary"
    - Labels → Text variant="label"

    **SystemSettingsClient.tsx (236 lines):**
    - System settings form → Stack of form fields using Input/Select from Form
    - Section headers → Text variant="sectionTitle"
    - Toggle/checkbox areas → keep native but wrap in Inline for alignment
    - Action buttons → Button variants

    **PlatformAdminLayout.tsx (78 lines):**
    - Navigation links → TextLink or Button variant="ghost"
    - Layout container → Container or Stack
    - Active state styling → cn() utility
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. All 4 files import from @/components/ui/.</verify>
  <done>All 4 platform admin files fully migrated to UI Kit. Admin area migration complete (UIKIT-02).</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. All 8 files import from @/components/ui/*
3. Dashboard pages use StatPill or Card for metrics
4. Settings pages use Form components (Input, Select, Textarea)
5. Layout components use UI Kit navigation primitives
</verification>

<success_criteria>
- All 6 small admin pages + 2 layout components migrated
- Combined with Plan 02, the entire admin area (UIKIT-02) is complete
- Consistent visual language across all admin pages
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-kit-integration/06-03-SUMMARY.md`
</output>
