---
phase: 06-ui-kit-integration
plan: 05
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/student/courses/StudentCoursesClient.tsx
  - app/student/exams/StudentExamsClient.tsx
  - app/student/exams/[examId]/take/ExamStartPage.tsx
  - app/student/attempts/[attemptId]/results/ResultsView.tsx
autonomous: true

must_haves:
  truths:
    - "Student courses page uses Card components for course list"
    - "Student exams page uses Badge for exam status and Card for exam cards"
    - "Exam start page uses Button for start action and Card for exam info"
    - "Results view uses Badge for score display and Card for feedback sections"
  artifacts:
    - path: "app/student/courses/StudentCoursesClient.tsx"
      provides: "Migrated student courses page"
      contains: "import.*Card.*from.*@/components/ui/Card"
    - path: "app/student/exams/StudentExamsClient.tsx"
      provides: "Migrated student exams list"
      contains: "import.*Badge.*from.*@/components/ui/Badge"
    - path: "app/student/exams/[examId]/take/ExamStartPage.tsx"
      provides: "Migrated exam start page"
      contains: "import.*Button.*from.*@/components/ui/Button"
    - path: "app/student/attempts/[attemptId]/results/ResultsView.tsx"
      provides: "Migrated results view"
      contains: "import.*Text.*from.*@/components/ui/Text"
  key_links:
    - from: "app/student/attempts/[attemptId]/results/ResultsView.tsx"
      to: "components/ui/Badge.tsx"
      via: "score badges"
      pattern: "import.*Badge.*from.*@/components/ui/Badge"
---

<objective>
Migrate all 4 student-facing pages to UI Kit components.

Purpose: Student pages need consistent visual language matching admin and teacher pages. Covers UIKIT-04 requirement.

Output: 4 fully migrated student pages.
</objective>

<execution_context>
@C:\Users\hugol\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hugol\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/teacher/courses/TeacherCoursesClient.tsx (reference implementation)
@components/ui/Button.tsx
@components/ui/Card.tsx
@components/ui/Text.tsx
@components/ui/Layout.tsx
@components/ui/Badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate StudentCoursesClient.tsx and StudentExamsClient.tsx</name>
  <files>
    app/student/courses/StudentCoursesClient.tsx
    app/student/exams/StudentExamsClient.tsx
  </files>
  <action>
    **StudentCoursesClient.tsx (385 lines):**
    Student's course list — similar structure to TeacherCoursesClient:
    - Page header → Text variant="pageTitle"
    - Search → SearchField
    - Course cards → Grid + Card + CardBody
    - Course code → Badge variant="info" or keep CourseCodeBadge if used
    - Exam count per course → StatPill or Text variant="caption"
    - Empty state → EmptyState with descriptive message
    - Navigation links → TextLink for course detail links
    - Card click → Card with interactive="subtle" and onClick

    **StudentExamsClient.tsx (353 lines):**
    Student's exam list:
    - Page header → Text variant="pageTitle"
    - Exam cards → Grid or Stack of Card + CardBody
    - Exam status (upcoming, in progress, completed) → StatusBadge or Badge
    - Due date/time → Text variant="caption"
    - Action button (Start, Continue, View Results) → Button variants
    - Score display (for completed) → Badge variant="success"/"warning" based on score
    - Empty state → EmptyState
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. Both files import from @/components/ui/.</verify>
  <done>StudentCoursesClient and StudentExamsClient fully migrated to UI Kit.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ExamStartPage.tsx and ResultsView.tsx</name>
  <files>
    app/student/exams/[examId]/take/ExamStartPage.tsx
    app/student/attempts/[attemptId]/results/ResultsView.tsx
  </files>
  <action>
    **ExamStartPage.tsx (261 lines):**
    Exam info display before starting:
    - Exam title → Text variant="pageTitle"
    - Exam details (duration, question count, instructions) → Card + CardBody with Stack
    - Duration display → Text variant="sectionTitle" or StatPill
    - Instructions text → Text variant="body"
    - Start button → Button variant="primary" size="md"
    - Warning text → Text variant="caption" with className for color
    - Timer info → Badge variant="warning"
    - Back link → TextLink or Button variant="secondary"

    **ResultsView.tsx (264 lines):**
    Student's graded results display:
    - Overall score header → Inline with Text + Badge for total score
    - Per-question sections → Stack of Card + CardBody
    - Question text → Text variant="body" (keep MathRenderer intact)
    - Student answer display → Surface tone="subtle" + Text (keep MathRenderer)
    - Score per question → Badge (success for full marks, warning for partial, neutral for zero)
    - AI feedback → Card + CardBody with Text variant="body"
    - AI/Human indicator → Badge variant="info" for AI
    - Color-coded score borders → keep existing color logic, apply via className on Card

    IMPORTANT: Preserve MathRenderer usage in both files. Do not modify math rendering, timer logic, or exam submission handlers.
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. Both files import from @/components/ui/. MathRenderer imports preserved.</verify>
  <done>ExamStartPage and ResultsView fully migrated. UIKIT-04 (student pages) complete. Math rendering and timer logic preserved.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. All 4 student files import from @/components/ui/*
3. MathRenderer integration preserved in ExamStartPage and ResultsView
4. Score color coding preserved in ResultsView
5. Timer/submission logic unaffected in ExamStartPage
</verification>

<success_criteria>
- All 4 student pages use UI Kit components (UIKIT-04 complete)
- Consistent card, badge, button patterns across student experience
- Math rendering and exam logic unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-kit-integration/06-05-SUMMARY.md`
</output>
